import React from 'react';
import { Form2Props } from './Form2.propTypes';
import styles from './Form2.styles.scss';

export const Form2 = props => {
    return (
        <div>
            Form2 component atom
        </div>
    );
};

Form2.propTypes = Form2Props.propTypes;
Form2.defaultProps = Form2Props.defaultProps;
